'use client';

import React from 'react';

export const Navbar = () => {
  return (
    <nav className="w-full h-16 border-b border-slate-800 bg-slate-900 flex items-center px-6 shrink-0">
      {/* Nothing at the moment */}
    </nav>
  );
};
